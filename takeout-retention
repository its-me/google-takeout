#!/usr/bin/env bash
set -euo pipefail

DRY_RUN=false

while [[ $# -gt 0 ]]; do
  case "$1" in
    --dry-run|-n) DRY_RUN=true; shift ;;
    *) echo "Usage: takeout-retention [--dry-run|-n]" >&2; exit 1 ;;
  esac
done

TAKEOUT_RCLONE_UNION_REMOTE_NAME="${TAKEOUT_RCLONE_UNION_REMOTE_NAME:-}"
TAKEOUT_RETENTION_KEEP_LAST="${TAKEOUT_RETENTION_KEEP_LAST:-10}"

[[ -z "$TAKEOUT_RCLONE_UNION_REMOTE_NAME" ]] && { echo "Error: TAKEOUT_RCLONE_UNION_REMOTE_NAME is not set" >&2; exit 1; }

union="${TAKEOUT_RCLONE_UNION_REMOTE_NAME}:"

# Retention rules:
#   1. Always keep files dated on the 1st or 16th of any month
#   2. Always keep the most recent $TAKEOUT_RETENTION_KEEP_LAST files (rules 1+2 overlap — 1st/16th count toward the 10)
apply_retention() {
  echo "[INFO] Processing $union"

  mapfile -t files < <(rclone lsf "$union" --files-only --max-depth 1 --include "*.tar.xz.age" | sort -r)

  if [[ ${#files[@]} -eq 0 ]]; then
    echo "[INFO] No backups found"
    return
  fi

  echo "[INFO] Found ${#files[@]} backup(s)"

  local rank=0
  local deleted=0
  for file in "${files[@]}"; do
    rank=$((rank + 1))

    date_str=$(echo "$file" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' | head -1)
    if [[ -z "$date_str" ]]; then
      echo "[WARN]   $file — cannot parse date, skipping"
      continue
    fi

    day="${date_str##*-}"
    keep=false
    reasons=()

    [[ $rank -le $TAKEOUT_RETENTION_KEEP_LAST ]] && { keep=true; reasons+=("top $TAKEOUT_RETENTION_KEEP_LAST"); }
    [[ "$day" == "01" || "$day" == "16" ]] && { keep=true; reasons+=("day-of-month $day"); }

    if $keep; then
      reason=$(IFS=', '; echo "${reasons[*]}")
      echo "[KEEP]   $file  ($reason)"
    else
      deleted=$((deleted + 1))
      if $DRY_RUN; then
        echo "[DELETE] $file  (would delete)"
      else
        echo "[DELETE] $file"
        rclone deletefile "${union}${file}"
      fi
    fi
  done

  if $DRY_RUN; then
    echo "[INFO] Dry run complete — $deleted file(s) would be deleted"
  else
    echo "[INFO] Done — $deleted file(s) deleted"
  fi
}

$DRY_RUN && echo "[INFO] Dry run — no files will be deleted"
echo ""
apply_retention
